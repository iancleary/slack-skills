[package]
name = "slack-query"
version = "1.0.1"
edition = "2024"
description = "Deterministic Slack retrieval CLI"
license = "MIT"
repository = "https://github.com/iancleary/slack-skills"
readme = "../../README.md"
publish = false

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
slack-core = { path = "../slack-core" }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
toml = "0.8"
url = "2"
